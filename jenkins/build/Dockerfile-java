FROM openjdk:8-jre-alpine

RUN mkdir /app

RUN apk update && apk upgrade && apk add python && apk add --update py-pip && apk add --update zlib && apk add --update sqlite


COPY *.jar /app/app.jar

CMD java -jar /app/app.jar
