FROM openjdk:8-jre-alpine

RUN mkdir /app

RUN apk upgrade && apk add python2-dev && apk add --update py2-pip && apk add gcc

RUN pip2 install --upgrade pysqlite

COPY *.jar /app/app.jar

CMD java -jar /app/app.jar
